# Indexing Service (planned)

This area will contain the workers responsible for:

- parsing PDFs/HTML (text + OCR + tables)
- normalization
- chunking (section-aware, citation-friendly)
- embeddings
- indexing into Qdrant (vector) and OpenSearch (BM25)

